<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>K Shop</title>
        <link rel="stylesheet" href="css/bootstrap.min.css"/>
        <script src="js/jquery-3.4.1.slim.min.js" ></script>
        <script src="js/popper.min.js"></script>
        <script src="js/bootstrap.min.js"></script>
        <link rel="stylesheet" href="css/all.css"/>
    </head>
    <body>
        <%- include('navbar') %>
        <div class="container">
            <h2>จัดการสินค้า</h2>
            <p>เพิ่ม ลบ แก้ไขข้อมูลในฐานข้อมูล <a href="/logout" class="btn btn-danger"> ออกจากระบบ</a></p>            
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>ชื่อสินค้า</th>
                        <th>ราคา</th>
                        <th>รายละเอียด</th>
                        <th>ลบ</th>
                        <th>แก้ไข</th>
                    </tr>
                </thead>
                <tbody>
                    <% Object.values(product).forEach(function(item){ %>
                    <tr>
                        <td><%= item.name %></td>
                        <td><%= item.price %></td>
                        <td><%= item.detail%></td>
                        <td><a href="/delete/<%=item._id%>" onclick="return confirm('คุณต้องการลบหรือไม่?')" class="btn btn-danger">ลบ</button></td>
                        <td>
                            <form action="/edit" method="post">
                                <input type="hidden" name="edit_id" value="<%= item._id %>">
                                <button type="submit" class="btn btn-info">แก้ไข</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </body>
</html>
